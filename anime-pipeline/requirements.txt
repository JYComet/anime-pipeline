# ============================================================
# Anime Pipeline — Core Dependencies
# ============================================================
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
requests>=2.31.0
urllib3>=2.0.0
numpy>=1.24.0
soundfile>=0.12.0
librosa>=0.10.0
python-multipart>=0.0.9

# PyTorch — install with CUDA support for GPU acceleration:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
# For RTX 50-series (Blackwell, sm_120), use CUDA 13.x:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu130
torch>=2.0.0
torchaudio>=2.0.0

# ClearVoice dependencies (speech enhancement / super resolution)
einops>=0.8.0
rotary_embedding_torch>=0.8.0
torchinfo>=1.8.0
yamlargparse>=0.3.0

# Music separation (BGM removal)
demucs>=4.0

# Optional: torrent file list parsing (pip install bencodepy)
# bencodepy>=0.4.0

# MFA Chinese alignment — tokenization + pinyin conversion
jieba>=0.42.0
pypinyin>=0.51.0

# Optional: ONNX Runtime for gender detection (pip install onnxruntime)
# onnxruntime>=1.18.0