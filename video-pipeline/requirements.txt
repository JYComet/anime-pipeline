# ============================================================
# Video Pipeline — Dependencies
# ============================================================
# Python 基础依赖

# YAML 配置文件解析
pyyaml>=6.0

# 音频处理
numpy>=1.24.0
soundfile>=0.12.0

# ============================================================
# PyTorch (GPU 加速)
# ============================================================
# 根据你的 CUDA 版本选择安装方式:
#
# CUDA 12.6 (推荐，RTX 30/40 系列):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#
# CUDA 13.x (RTX 50 系列 Blackwell):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cu130
#
# CPU only (不推荐，BGM 分离极慢):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 一键安装脚本会自动安装适合的版本，无需手动安装。
torch>=2.0.0
torchaudio>=2.0.0

# ============================================================
# 模型部署依赖
# ============================================================

# BGM 分离 — Meta Demucs (htdemucs_ft)
# 这是核心依赖，用于人声/背景音乐分离
demucs>=4.0

# Demucs 间接依赖 (通过 demucs 自动安装，列在此处以供参考)
# - dacvae, einops, julius, lameenc, openunmix, submitit, treetable
